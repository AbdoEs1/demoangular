import { ColorPriceDirective } from './color-price.directive';

describe('ColorPriceDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorPriceDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
